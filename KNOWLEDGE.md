# DesignFlow 产品知识库

> LLM 对话注入用。每次功能迭代或 bug 修复后同步更新此文件。

---

## 平台概述

DesignFlow 是一个 AI 驱动的电商设计资产平台，帮助运营/设计人员通过对话式操作快速批量合成产品海报。

核心能力：AI 对话 + Penpot 模板库 + AI 生图 + 产品图库匹配，实现素材半自动化生产。

---

## 核心工作流程

1. 在左侧模板库选择一个 Penpot 设计模板
2. 上传产品需求表格（Excel/CSV），AI 自动解析字段并匹配产品图
3. 通过对话指令触发生图 / 合成任务
4. 导出 PNG 或打包下载

---

## 指令参考

### 通用对话

直接在输入框打字即可，AI 助手会回答平台使用问题、给出操作建议。

---

### AI 生图

| 指令 | 说明 |
|------|------|
| `/Gpt image 2 <描述>` | 文生图，超强中文理解，适合生成海报等 |
| `/Nano Banana pro <描述>` | 图生图 / 图片编辑，高度一致性，适合换产品、改局部 |

**参考图与货号索引**

- 上传参考图：点击输入框旁的附件按钮，或直接粘贴图片（最多 4 张）
- 货号索引：在 prompt 中输入 `[SKU编号]`，系统自动在本地图库查找白底产品图并上传
- 一双鞋角度索引：输入 `[SKU编号一双鞋角度]`
- 货号索引会替换为"这个白底产品图"增强 prompt
- 支持多个货号，如 `把模特衣服换成[ATSW042-4]`

**会话上下文**

首次生图必须带 `/` 指令（如 `/Gpt image 2` 或 `/Nano Banana pro`），系统会自动记住你选择的模型。

之后的修改直接输入需求即可，**不需要**重复 `/` 指令。例如：

```
第1轮：/Gpt image 2 生成一张篮球鞋海报
第2轮：把背景换成蓝色球场        ← 无需 / 指令，自动复用 Gpt Image 2
第3轮：加一行促销文案             ← 继续复用
```

系统在后台会自动做两件事：

1. 用 LLM 将你的简短修改改写为完整的生图 prompt（结合对话历史理解上下文）
2. 将上一张结果图作为参考图传递给生图模型

中途可以随时切换模型，上下文继续保留：

```
第4轮：/Nano Banana pro 把鞋底改成红色   ← 显式切换模型
第5轮：调亮一点                              ← 继续用 Nano Banana Pro
```

上下文注入对用户完全透明，聊天记录始终显示你输入的原始文字。切换会话或清空聊天后上下文重置。

**不需要上下文时**

如果当前会话已经有生图历史，但你想要一张全新、不受上文影响的图，输入 **重新生成** + 描述即可。系统会跳过上下文，以 Gpt Image 2 全新开始：

```
重新生成 一张干净的白色背景产品图
```

也支持"重新生图""全新生成"等同义关键词。

---

### 特殊品合成

**前提**：先在左侧模板库选中特殊品模板，再输入指令。

**两种模式**

- **`/特殊品`** — 基础版，填入产品图 + 文案即可合成导出
- **`/特殊品（完整）`** — 完整版，额外包含场景图 slot，导出前自动刷新 Penpot 布局确保排版正确

**指令格式**

```
/特殊品 SKU编号，产品名称，发售时间
/特殊品（完整） SKU编号，产品名称，发售时间
```

参数之间用中文逗号（，）分隔：

- **SKU编号**：货号，系统自动从图库匹配白底图和场景图
- **产品名称**：显示在成品上的文案
- **发售时间**：显示在成品上的时间文案（如不需要可省略）

**示例**

```
/特殊品（完整） ABAW023-6，飓风2 极限之力 雷暴篮球专业比赛鞋，5月20日 10点发售
```

**合成结果**

- 生成的多张成品图自动出现在聊天窗口中
- 支持单张下载、打包下载（ZIP）、在 Penpot 中编辑

---

### 通用合成

上传表格并选中模板后，点击 AI 回复中的「开始生图」按钮即可触发生成。

---

### 花瓣下载

下载花瓣网设计素材到本地。支持 Pin ID、URL 链接、指定格式下载。

**格式**

```
/花瓣下载 <花瓣URL或PinID> [格式后缀]
```

**示例**

- `/花瓣下载 https://huaban.com/pins/1234567890`
- `/花瓣下载 1234567890` （简写，直接输入 Pin ID）
- `/花瓣下载 1234567890 PSD` （指定下载 PSD 格式）

**已知情况**

- 不用事先选模板，直接输入指令即可
- 如果花瓣网页面包含多格式选项，AI 助手会提示你选择格式
- 下载完成后会显示文件名和大小信息

---

### 导出相关

- `/导出PNG` — 下载合成结果
- `/切九宫格` — 将结果裁切为 3×3 九宫格

---

## AI 生图详细说明

### 模型选择

**Gpt Image 2**

超强文本理解和中文渲染，适合从文字描述生成图片、根据参考图生成海报。

**Nano Banana Pro**

高度一致性保持，适合图片编辑场景（如更换产品、修改局部）。

> 不建议用"图1""图2"等词引导，因为图片顺序可能因网络问题变化。

### 尺寸与分辨率

| 比例 | 可选分辨率 |
|------|-----------|
| auto | 1K / 2K / 4K（自动） |
| 1:1 | 1K (1024×1024), 2K (2048×2048), 4K (4096×4096) |
| 3:4 | 1K (768×1024), 2K (1536×2048), 4K (2448×3264) |
| 9:16 | 1K (1080×1920), 2K (1152×2048), 4K (2160×3840) |
| 5:4 | 1K (1280×1024), 2K (2560×2048) — 仅 Gpt Image 2 |

Nano Banana Pro 最大宽或高不超过 4096px。

---

## 模板系统

模板存储在 Penpot，设计师自行维护。程序通过 Penpot API 拉取模板列表和缩略图。

### Slot 命名规范

需要被程序替换的图层使用 `slot/` 前缀命名：

- `slot/product_1/image` — 第 1 格产品图
- `slot/product_1/name` — 第 1 格产品名称
- `slot/product_1/price` — 第 1 格价格
- `slot/product_1/tag` — 第 1 格标签
- `slot/product_1/spec` — 第 1 格规格
- `slot/poster` — 海报图
- `slot/blank` — 空白格（跳过不处理）

### 特殊品模板

- 来自 Penpot 中的特殊品模板文件
- 需要同时导出多个画板
- 支持自动填入白底图、场景图、文案
- 自动拆分分段文案、增加变体版本
- 自动按规则命名导出 png / jpg

---

## 产品图库

- 统一存放在 `product-library/` 目录
- 按图片类型分文件夹：

| 文件夹 | 用途 |
|--------|------|
| `PNG/` | 透明背景产品图 |
| `White_Base/` | 白底产品图 |
| `White_Basex2/` | 2 倍白底产品图 |
| `Model_Images/` | 模特场景图 |
| `PNG_Shadow/` | 带阴影产品图 |
| `场景图/Banner/` | 横版场景图 |
| `场景图/Poster/` | 竖版场景图 |

- 按货号匹配：文件名 = 货号
- 建议使用透明背景 PNG，分辨率不低于 800×800px

---

## Loom 插件

定位是轻量极简的 AI 绘画工具，不限设备，所有电脑都能安装。

- 调用满血版 Nano Banana Pro 模型，原生 4K 分辨率
- 特殊品模板自动导出到场景图文件夹
- 特殊品区域打开模板（以副本形式打开，不会破坏母版）

### 安装方式

1. 访问共享目录 `\\192.168.150.52\素材库\工具\loom`，拷贝最新版文件夹（如 `loom-ai-1.0.10`）
2. **Windows**：粘贴到 `C:\Program Files (x86)\Common Files\Adobe\CEP\extensions`
3. **Mac**：粘贴到 `~/Library/Application Support/Adobe/CEP/extensions`
   - 访达 → 前往 → 前往文件夹，输入路径
4. 重启 Photoshop
5. 在 PS 菜单栏点击 **窗口 → 扩展（旧版）→ Loom AI** 打开插件

### 已知问题

- 偶尔不能输入中文（系统层 bug），从别处复制或重新开关插件可解决
- 重绘整图时可能有细微变化，可增加蒙版锁定不需要重绘的区域
- 扩图可能进行不必要的缩放，建议先用 prompt 锁定效果

---

## 用户系统

- 轻登录：输入名字即可进入
- 每个用户有独立的历史记录和 AI 生图存储
- 管理员可查看所有用户的记录
- 设置头像：在聊天窗口上传一张图片，输入"设为头像"即可更新头像，未设置时显示用户名首字

---

## 常见问题

### 怎么开始生图？

先选左侧模板，上传产品表格，然后输入 `/Gpt image 2 <描述>` 或 `/Nano Banana pro <描述>`。

### 怎么换模板？

直接在左侧模板库点击另一个模板即可，无需刷新。

### 怎么在 Penpot 里编辑结果？

特殊品合成完成后，结果消息中会有「在 Penpot 中编辑」按钮。

### AI 生图失败怎么办？

检查 `.env` 中 `AI_IMAGE_API_KEY` 是否配置正确、余额是否充足。失败时系统会显示具体错误信息。

### 输入框怎么调整大小？

拖拽输入框顶部的横线手柄上下调整高度，最低 140px 确保按钮可见。

---

## 更新日志

- 2025-05-24: 新增动态用户头像系统，支持聊天窗口"设为头像"
- 2025-05-24: 输入框支持拖拽调整高度
- 2025-05-23: 新增 AI 生图会话上下文（LLM 改写 prompt + 上一张图作为参考）
- 2025-05-23: 新增智能指令路由（无 / 指令时复用上一个生图模型）
- 2025-05-22: 修复特殊品导出前 Penpot 布局未刷新问题
- 2025-05-22: 完善特殊品导出命名与打包下载格式规则
- 2025-05-22: 完善 Nano Banana Pro 尺寸与指令标准化
- 2025-05-22: 新增素材引用与指令可用性控制
