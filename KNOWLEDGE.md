# DesignFlow 产品知识库

> LLM 对话注入用。每次功能迭代或 bug 修复后同步更新此文件。

---

## 平台概述

DesignFlow 是一个 AI 驱动的电商设计资产平台，帮助运营/设计人员通过对话式操作快速批量合成产品海报。

核心能力：AI 对话 + Penpot 模板库 + AI 生图 + 产品图库匹配，实现素材半自动化生产。

---

## 核心工作流程

1. 在左侧模板库选择一个 Penpot 设计模板
2. 上传产品需求表格（Excel/CSV），AI 自动解析字段并匹配产品图
3. 通过对话指令触发生图/合成任务
4. 导出 PNG 或打包下载

---

## 指令参考

### 通用对话

直接在输入框打字即可，AI 助手会回答平台使用问题、给出操作建议。

### AI 生图

| 指令 | 说明 |
|------|------|
| `/Gpt image 2 <描述>` | 文生图，超强中文理解，适合生成海报等 |
| `/Nano Banana pro <描述>` | 图生图/图片编辑，高度一致性，适合换产品、改局部 |

- 支持上传参考图（最多 4 张），通过附件按钮或粘贴
- 支持货号索引：输入 `[SKU编号]` 自动查找并上传白底产品图
- 支持一双鞋角度索引：输入 `[SKU编号一双鞋角度]`
- 同一个会话内再次生图时，自动继承上下文（上一张图作为参考 + LLM 改写 prompt）
- 二次生成无需重复 / 指令，直接输入修改需求即可
- 可随时用 `/Gpt image 2` 或 `/Nano Banana pro` 切换模型，上下文不丢失

### 特殊品合成

| 指令 | 说明 |
|------|------|
| `/特殊品 SKU，文案，时间文案` | 需要先选中特殊品模板，自动合成并导出 |
| `/特殊品（完整） SKU，文案，时间文案` | 完整版，包含场景图 slot，导出前自动刷新 Penpot 布局 |

- 特殊品模板在左侧模板库选择
- 合成结果支持单张下载、打包下载、在 Penpot 中编辑

### 通用合成

上传表格并选中模板后，点击 AI 回复中的「开始生图」按钮即可触发生成。

### 导出相关

- `/导出PNG` — 下载合成结果
- `/切九宫格` — 将结果裁切为 3×3 九宫格

---

## AI 生图详细说明

### 模型选择

- **Gpt Image 2**：超强文本理解和中文渲染，适合从文字描述生成图片、根据参考图生成海报。
- **Nano Banana Pro**：高度一致性保持，适合图片编辑场景（如更换产品、修改局部）。不建议用"图1""图2"等词引导，因为图片顺序可能因网络问题变化。

### 尺寸与分辨率

| 比例 | 可选分辨率 |
|------|-----------|
| auto | 1K / 2K / 4K（自动） |
| 1:1 | 1K (1024×1024), 2K (2048×2048), 4K (4096×4096) |
| 3:4 | 1K (768×1024), 2K (1536×2048), 4K (2448×3264) |
| 9:16 | 1K (1080×1920), 2K (1152×2048), 4K (2160×3840) |
| 5:4 | 1K (1280×1024), 2K (2560×2048) — 仅 Gpt Image 2 |

- Nano Banana Pro 最大宽或高不超过 4096px

### 参考图与货号索引

- 上传参考图：点击输入框旁的附件按钮，或直接粘贴图片（最多 4 张）
- 货号索引：在 prompt 中输入 `[SKU编号]`，系统自动在本地图库查找白底产品图并上传
- 一双鞋角度：输入 `[SKU编号一双鞋角度]`
- 货号索引会替换为"这个白底产品图"增强 prompt
- 支持多个货号，如 `把模特衣服换成[ATSW042-4]`

### 会话上下文

- 首次生图必须带 `/` 指令
- 同一会话内的后续修改**不需要** `/` 指令，直接输入修改需求即可
- 系统自动用上一个模型 + 上一张结果图作为参考图
- 可以用 `/` 指令显式切换模型，上下文继续保留
- 上下文注入对用户透明，聊天记录始终显示原始输入

---

## 模板系统

模板存储在 Penpot，设计师自行维护。程序通过 Penpot API 拉取模板列表和缩略图。

### Slot 命名规范

需要被程序替换的图层使用 `slot/` 前缀命名：

- `slot/product_1/image` — 第1格产品图
- `slot/product_1/name` — 第1格产品名称
- `slot/product_1/price` — 第1格价格
- `slot/product_1/tag` — 第1格标签
- `slot/product_1/spec` — 第1格规格
- `slot/poster` — 海报图
- `slot/blank` — 空白格（跳过不处理）

### 特殊品模板

- 来自 Penpot 中的特殊品模板文件
- 需要同时导出多个画板
- 支持自动填入白底图、场景图、文案
- 自动拆分分段文案、增加变体版本
- 自动按规则命名导出 png/jpg

---

## 产品图库

- 统一存放在 `product-library/` 目录
- 按图片类型分文件夹：`PNG/`、`White_Base/`、`White_Basex2/`、`Model_Images/`、`PNG_Shadow/`、`场景图/Banner/`、`场景图/Poster/`
- 按货号匹配：文件名 = 货号
- 建议使用透明背景 PNG，分辨率不低于 800×800px

---

## Loom 插件

- 定位是轻量极简的 AI 绘画工具
- 不限设备，所有电脑都能安装
- 调用满血版 Nano Banana Pro 模型，原生 4K 分辨率
- 自动导出到场景图文件夹
- 特殊品区域打开模板（以副本形式打开，不会破坏母版）

已知问题：
- 偶尔不能输入中文（系统层 bug），从别处复制或重新开关插件可解决
- 重绘整图时可能有细微变化，可增加蒙版锁定不需要重绘的区域
- 扩图可能进行不必要的缩放，建议先用 prompt 锁定效果

---

## 用户系统

- 轻登录：输入名字即可进入
- 每个用户有独立的历史记录和 AI 生图存储
- 管理员可查看所有用户的记录

---

## 常见问题

### 怎么开始生图？
先选左侧模板，上传产品表格，然后输入 `/Gpt image 2 <描述>` 或 `/Nano Banana pro <描述>`。

### 怎么换模板？
直接在左侧模板库点击另一个模板即可，无需刷新。

### 怎么在 Penpot 里编辑结果？
特殊品合成完成后，结果消息中会有「在 Penpot 中编辑」按钮。

### AI 生图失败怎么办？
检查 `.env` 中 `AI_IMAGE_API_KEY` 是否配置正确、余额是否充足。失败时系统会显示具体错误信息。

### 输入框怎么调整大小？
拖拽输入框顶部的手柄横线上下调整高度。

---

## 更新日志

- 2025-05-23: 新增 AI 生图会话上下文（LLM 改写 prompt + 上一张图作为参考）
- 2025-05-23: 新增智能指令路由（无 / 指令时复用上一个生图模型）
- 2025-05-23: 输入框支持拖拽调整高度
- 2025-05-22: 修复特殊品导出前 Penpot 布局未刷新问题
- 2025-05-22: 完善特殊品导出命名与打包下载格式规则
- 2025-05-22: 完善 Nano Banana Pro 尺寸与指令标准化
- 2025-05-22: 新增素材引用与指令可用性控制
