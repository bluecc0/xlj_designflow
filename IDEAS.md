# Ideas

记录当前项目里已经讨论过、技术上有方向、但暂时搁置或未排期的想法。

## 1. AI 生图转 PSD / image2psd

- 状态：`可行，暂缓`
- 背景：
  目前 AI 生图已经接通 APIMart，但“生成后转为 PSD”这条链路还没有重新上线。
- 讨论结论：
  - 直接把一张成品图“逆向拆成很多层”不稳定。
  - 更靠谱的是先有一张基准图，再做后处理拆层。
  - 真正高质量的分层 PSD，更适合“生成时就按层产出”，而不是成图后硬拆。
- 当前建议路线：
  1. `转成PSD`：定位为已有成品图的粗拆工具。
  2. `生成分层PSD`：按 layer manifest 分层生成，再组装 PSD。
- 参考：
  - `bggg-creator-image2psd` 项目
  - 关键原则：保留原图像素、全画布透明层、少重绘、多局部修补
- 暂缓原因：
  产品路径还没完全定，当前先不继续做半成品。

## 2. 内网素材站代办下载

- 状态：`可行，暂缓`
- 背景：
  有一个已购买会员权限的素材下载网站，只有一台固定电脑登录账号，希望其他内网用户通过聊天提交 URL，由本机代办下载。
- 讨论结论：
  - 不适合做成“万能下载代理”。
  - 应该做成“单网站专用下载 worker”。
  - 更适合浏览器自动化，而不是直接 requests 抓取。
- 当前建议路线：
  1. 聊天提交 URL
  2. 后端创建下载任务
  3. 本机浏览器自动化执行下载
  4. 下载完成后回传文件/链接
- 预计难点：
  - 多种素材类型：PNG / PSD / ZIP / 视频等
  - 下载按钮逻辑不统一
  - 可能有弹窗、二次确认、规格选择
  - 登录态依赖本机浏览器
- 建议边界：
  - 只支持白名单域名
  - 一次只支持一个链接
  - 先只支持一个素材站
  - 记录下载日志
- 暂缓原因：
  交互和站点流程复杂，先不在当前阶段展开。

## 3. 模板库文件夹分类

- 状态：`方向已定，后续继续`
- 背景：
  模板来自 Penpot，但不希望为了目录结构去改 Penpot 本身。
- 讨论结论：
  - Penpot 继续只负责模板内容。
  - 前端单独维护“文件夹/分类”元数据。
- 当前建议路线：
  - 在模板库 TAB 下做一级折叠分组
  - 每个文件夹点击展开，展示其下模板卡片
  - 默认保留“未分类”
- 数据存储建议：
  - 第一版：`template_folders.json`
  - 后续如需可编辑：再升级到 SQLite/API
- 暂未开始原因：
  当前优先级还没切到模板分类体验完善。

## 4. 登录与用户隔离后续增强

- 状态：`已做基础版，后续可增强`
- 当前已完成：
  - 轻登录
  - 用户映射
  - 历史记录按用户隔离
  - AI 图按用户目录存储
- 后续想法：
  - 顶部身份区可以继续精简
  - 如果将来外网访问，需要升级认证方式
  - AI 图访问后续可改为受控下载，而不是纯静态路径

## 5. 右上角状态区增强

- 状态：`进行中，后续可继续打磨`
- 当前已完成：
  - 文字 chip 改成 icon 形式
  - AI 服务商 icon 已接入真实 APIMart 状态
  - 余额改成点击 icon 后查看，不默认显示
- 后续想法：
  - tooltip / 弹层文案进一步正式化
  - 增加最后检测时间
  - 更明确区分：未配置 / key 失效 / 服务异常 / 余额不足

## 6. APIMart 健康检查与模型状态

- 状态：`已接入基础版，后续可增强`
- 当前已完成：
  - `/health` 已接入 APIMart `GET /v1/user/balance`
  - 右上角可显示 AI 服务商状态
- 后续想法：
  - 根据不同模型或服务商做更细粒度状态
  - 如果后面增加第二家服务商，需要拆成 provider 级配置

## 7. 外网访问 / 反向代理准备

- 状态：`可行，暂缓`
- 背景：
  未来可能通过服务器反向代理支持外网访问，但素材库、后端执行和 Penpot 仍然保留在本机/内网。
- 讨论结论：
  - 业务和数据继续放在正式机
  - 外网入口只做代理，不落业务数据
- 当前建议路线：
  - 提前区分 internal / public URL
  - 外网阶段优先考虑隧道 / 反代，而不是迁移数据
- 暂缓原因：
  当前以内网稳定使用为主，先不展开部署改造。

## 8. 正式环境迁移与更新

- 状态：`已讨论，待整理脚本`
- 结论摘要：
  - 正式机和开发机分离
  - Penpot 作为 Docker 服务管理
  - 项目代码通过 git pull 更新
  - 数据目录与代码目录分开
- 还没做的东西：
  - `start-prod.bat`
  - `update-prod.bat`
  - 正式环境迁移清单

## 9. 聊天输入框增强

- 状态：`持续迭代`
- 已完成：
  - 修复长文本输入时文字消失 / 光标乱跳
  - slash 指令和正文重新收回同一个输入框
- 后续可选想法：
  - 输入历史回填继续扩展成多条历史
  - `↑ / ↓` 浏览输入历史
  - 更明显但不打扰的 slash 命令视觉样式

## 10. 需要持续注意的技术债

- `frontend-dist` 仍是浏览器内 Babel 编译的静态前端，不是标准工程化构建
- `frontend-dist/build.py` 与当前页面结构不匹配，已知不可用
- 根目录仍有历史/备份产物，后续可以继续清理 demo 残留

## 备注

- 这份文件主要记录“已经讨论过的方向”和“暂缓原因”。
- 后续如果某条开始动手，可以把状态改成：
  - `排期中`
  - `开发中`
  - `已完成`
